import { Button } from '@components/ui/button'
import React from 'react'

const CreateReminder = () => {
  return <Button className='w-full'>Crear recordatorio</Button>
}

export default CreateReminder
